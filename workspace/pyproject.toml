[project]
name = "cdk-wrappper-workspace"
version = "0.1.0"
description = "example workspace for the cdk wrapper"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    # TODO aws_cli etc.
]
