[project]
name = "cdk-wrappper-workspace"
version = "0.1.0"
description = "example workspace for the cdk wrapper"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aws-cdk-lib>=2.220",
    "pydantic>=2.11",
    "pyyaml>=6.0",
]
